# Test overrides for security — never use real credentials here
ADMIN_USERNAME=testadmin
ADMIN_PASSWORD=testpassword

# Redirect inline persistence to a temp path during tests (overridden per test where needed)
mock.config.file=${java.io.tmpdir}/mock-endpoints-test.json
