# Test overrides for security — never use real credentials here
ADMIN_USERNAME=testadmin
ADMIN_PASSWORD=testpassword

# Persistence stays disabled in @SpringBootTest contexts; service-level persistence
# behaviour is exercised hermetically in MockEndpointServiceTest via temp dirs.
mock-server.persistence.enabled=false
